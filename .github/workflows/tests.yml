name: Tests

on:
  push:
    branches: ['**']
    paths:
      - 'libft/tests/**'
  pull_request:
    paths:
      - 'libft/tests/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run unit tests
        run: ./libft/tests/run_tests.sh
