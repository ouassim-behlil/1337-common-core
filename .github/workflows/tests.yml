name: Tests

on:
  push:
    branches: ['**']
    paths:
      - 'tests/**'
  pull_request:
    paths:
      - 'tests/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run unit tests
        run: ./tests/run_tests.sh
